{
  "name": "nhap-vai-ai-simulator",
  "version": "1.0.0",
  "description": "AI Roleplay Simulator Game, Nhập Vai A.I Simulator",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/bundle.js --platform=browser --format=esm --jsx=automatic --loader:.tsx=tsx"
  },
  "devDependencies": {
    "esbuild": "^0.21.4"
  }
}
